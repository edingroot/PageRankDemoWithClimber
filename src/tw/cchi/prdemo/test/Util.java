package tw.cchi.prdemo.test;

/*import java.util.ArrayList;
import java.util.List;
import java.util.Map;*/

public class Util {
    /* problem moving key
	public static void SortMapByValue(Map<Integer, Double> map, boolean isAsc) {
		int[] tmpKey = new int[2];
		double[] tmpVal = new double[2];
    	List<Integer> keys = new ArrayList<Integer>(map.keySet());
    	for (int i = 0; i < keys.size(); i++) {
    		for (int j = i; j < keys.size() - 1; j++) {
    			if ( isAsc ^ (map.get(keys.get(j)) > map.get(keys.get(j + 1)))) {
    				tmpKey[0] = keys.get(j);
    				tmpKey[1] = keys.get(j + 1);
    				tmpVal[0] = map.get(keys.get(j));
    				tmpVal[1] = map.get(keys.get(j + 1));
    				System.out.printf("[%d]=%f\n", tmpKey[0], tmpVal[1]);
    				System.out.printf("[%d]=%f\n", tmpKey[1], tmpVal[0]);
    				System.out.println("-----------");
    				map.put(tmpKey[0], tmpVal[1]);
    				map.put(tmpKey[1], tmpVal[0]);
    			}
    		}
    	}
    }*/
}